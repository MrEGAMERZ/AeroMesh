[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --ignore=tests/test_smoke_vggsfm.py
markers =
    gpu: mark test as requiring a CUDA GPU (use --run-gpu to include)
